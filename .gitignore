node_modules/
dist/
*.log

# Local launcher data - never stored in git.
# userdata holds the Microsoft auth session and must not be published.
userdata/
loaders/
java/
settings.json
# Holds the refresh tokens of every signed-in account.
accounts.json
# The player's own modpacks - their mods, configs and worlds.
instances.json
instances/
# The CurseForge API key. A secret, and tied to a person's account.
secrets.json
versions-cache.json
java-versions.json
# Fetched from Modrinth and kept only so the launcher works without a
# connection. Nothing here is written by hand.
modrinth-categories.json
modrinth-versions.json

# Local tooling notes and settings
.claude/
CLAUDE.md
