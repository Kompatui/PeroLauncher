node_modules/
dist/
*.log

# Локальные данные лаунчера — в git не хранятся.
# userdata содержит сессию авторизации Microsoft, её публиковать нельзя.
userdata/
settings.json

# Локальные настройки Claude Code
.claude/
